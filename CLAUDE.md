# Environment

Always use `hatch run` to execute commands (tests, scripts, etc.). Never use bare `python` or `pytest` directly.

```bash
# Examples
hatch run pytest tests/
hatch run python scripts/run_once.py
```
